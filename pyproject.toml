[project]
name = "gharchive-spark"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=3.5,<4",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"